# rougerRoll

一个简单的投掷色子 Web 小游戏。

## 运行方式

直接在浏览器中打开 `index.html`，或在项目目录执行：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 玩法

- 点击“投掷”按钮，随机获得 1~6 的点数。
- 页面会显示当前点数、总投掷次数、历史最高点数。
